---
title: İstemci ile Sunucu değerlendirmesi-EF Core
author: smitpatel
ms.date: 10/03/2019
ms.assetid: 8b6697cc-7067-4dc2-8007-85d80503d123
uid: core/querying/client-eval
ms.openlocfilehash: 3d70324f0b57a0ea9b165b5140a2154001c326f4
ms.sourcegitcommit: 708b18520321c587b2046ad2ea9fa7c48aeebfe5
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/09/2019
ms.locfileid: "72181909"
---
# <a name="client-vs-server-evaluation"></a><span data-ttu-id="1dd15-102">İstemci ile Sunucunun Karşılaştırmalı Değerlendirmesi</span><span class="sxs-lookup"><span data-stu-id="1dd15-102">Client vs. Server Evaluation</span></span>

<span data-ttu-id="1dd15-103">Genel bir kural olarak, Entity Framework Core sunucu üzerinde mümkün olduğunca bir sorguyu değerlendirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-103">As a general rule, Entity Framework Core attempts to evaluate a query on the server as much as possible.</span></span> <span data-ttu-id="1dd15-104">EF Core sorgunun bölümlerini, istemci tarafında değerlendirebileceği parametrelere dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="1dd15-104">EF Core converts parts of the query into parameters, which it can evaluate on the client side.</span></span> <span data-ttu-id="1dd15-105">Sorgunun geri kalanı (oluşturulan parametrelerle birlikte), sunucuda değerlendirilecek eşdeğer veritabanı sorgusunun belirlenmesi için veritabanı sağlayıcısına verilir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-105">The rest of the query (along with the generated parameters) is given to the database provider to determine the equivalent database query to evaluate on the server.</span></span> <span data-ttu-id="1dd15-106">EF Core, üst düzey projeksiyonda (temelde, `Select()` ' a yapılan son çağrı) kısmi istemci değerlendirmesini destekler.</span><span class="sxs-lookup"><span data-stu-id="1dd15-106">EF Core supports partial client evaluation in the top-level projection (essentially, the last call to `Select()`).</span></span> <span data-ttu-id="1dd15-107">Sorgudaki en üst düzey projeksiyon sunucuya çevrilemediği takdirde, EF Core sunucudan gerekli verileri alır ve sorgunun kalan bölümlerini değerlendirir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-107">If the top-level projection in the query can't be translated to the server, EF Core will fetch any required data from the server and evaluate remaining parts of the query on the client.</span></span> <span data-ttu-id="1dd15-108">EF Core, üst düzey projeksiyon dışında herhangi bir yerde, sunucuya çevrilemeyen bir ifade algılarsa, çalışma zamanı özel durumu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1dd15-108">If EF Core detects an expression, in any place other than the top-level projection, which can't be translated to the server, then it throws a runtime exception.</span></span> <span data-ttu-id="1dd15-109">Sorgunun ne EF Core sunucuya çevrilemeyecek olduğunu anlamak için [sorgunun nasıl çalıştığını](xref:core/querying/how-query-works) görün.</span><span class="sxs-lookup"><span data-stu-id="1dd15-109">See [how query works](xref:core/querying/how-query-works) to understand how EF Core determines what can't be translated to server.</span></span>

> [!NOTE]
> <span data-ttu-id="1dd15-110">Sürüm 3,0 ' den önce, sorgudaki her yerde desteklenen istemci değerlendirmesi Entity Framework Core.</span><span class="sxs-lookup"><span data-stu-id="1dd15-110">Prior to version 3.0, Entity Framework Core supported client evaluation anywhere in the query.</span></span> <span data-ttu-id="1dd15-111">Daha fazla bilgi için [önceki sürümler bölümüne](#previous-versions)bakın.</span><span class="sxs-lookup"><span data-stu-id="1dd15-111">For more information, see the [previous versions section](#previous-versions).</span></span>

## <a name="client-evaluation-in-the-top-level-projection"></a><span data-ttu-id="1dd15-112">Üst düzey projeksiyde istemci değerlendirmesi</span><span class="sxs-lookup"><span data-stu-id="1dd15-112">Client evaluation in the top-level projection</span></span>

> [!TIP]
> <span data-ttu-id="1dd15-113">Bu makalenin görüntüleyebileceğiniz [örnek](https://github.com/aspnet/EntityFramework.Docs/tree/master/samples/core/Querying) GitHub üzerinde.</span><span class="sxs-lookup"><span data-stu-id="1dd15-113">You can view this article's [sample](https://github.com/aspnet/EntityFramework.Docs/tree/master/samples/core/Querying) on GitHub.</span></span>

<span data-ttu-id="1dd15-114">Aşağıdaki örnekte, bir SQL Server veritabanından döndürülen blogların URL 'Lerini standartlaştırmak için bir yardımcı yöntem kullanılır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-114">In the following example, a helper method is used to standardize URLs for blogs, which are returned from a SQL Server database.</span></span> <span data-ttu-id="1dd15-115">SQL Server sağlayıcının bu yöntemin nasıl uygulandığı hakkında öngörü olmadığından, SQL 'e çevirmek mümkün değildir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-115">Since the SQL Server provider has no insight into how this method is implemented, it isn't possible to translate it into SQL.</span></span> <span data-ttu-id="1dd15-116">Sorgunun diğer tüm yönleri veritabanında değerlendirilir, ancak döndürülen `URL` ' ı bu yöntem aracılığıyla istemciye geçirme işlemi istemcide yapılır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-116">All other aspects of the query are evaluated in the database, but passing the returned `URL` through this method is done on the client.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientProjection)]

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientMethod)]

## <a name="unsupported-client-evaluation"></a><span data-ttu-id="1dd15-117">Desteklenmeyen istemci değerlendirmesi</span><span class="sxs-lookup"><span data-stu-id="1dd15-117">Unsupported client evaluation</span></span>

<span data-ttu-id="1dd15-118">İstemci değerlendirmesi yararlı olsa da, bazı durumlarda düşük performansa neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-118">While client evaluation is useful, it can result in poor performance sometimes.</span></span> <span data-ttu-id="1dd15-119">Aşağıdaki sorguyu göz önünde bulundurun. Bu, yardımcı yönteminin Şu anda bir WHERE filtresinde kullanıldığı bir yerdir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-119">Consider the following query, in which the helper method is now used in a where filter.</span></span> <span data-ttu-id="1dd15-120">Filtre veritabanına uygulanamadığından, filtrenin istemciye uygulanması için tüm verilerin belleğe çekililmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-120">Because the filter can't be applied in the database, all the data needs to be pulled into memory to apply the filter on the client.</span></span> <span data-ttu-id="1dd15-121">Filtre ve sunucudaki veri miktarına bağlı olarak, istemci değerlendirmesi kötü performansa neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-121">Based on the filter and the amount of data on the server, client evaluation could result in poor performance.</span></span> <span data-ttu-id="1dd15-122">Entity Framework Core, bu tür istemci değerlendirmesini engeller ve bir çalışma zamanı özel durumu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1dd15-122">So Entity Framework Core blocks such client evaluation and throws a runtime exception.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientWhere)]

## <a name="explicit-client-evaluation"></a><span data-ttu-id="1dd15-123">Açık istemci değerlendirmesi</span><span class="sxs-lookup"><span data-stu-id="1dd15-123">Explicit client evaluation</span></span>

<span data-ttu-id="1dd15-124">Aşağıdaki gibi belirli durumlarda istemci değerlendirmesi için açıkça zorlamanız gerekebilir</span><span class="sxs-lookup"><span data-stu-id="1dd15-124">You may need to force into client evaluation explicitly in certain cases like following</span></span>

- <span data-ttu-id="1dd15-125">Verilerin miktarı küçüktür. böylece, istemcide değerlendirmek büyük bir performans cezası olmaz.</span><span class="sxs-lookup"><span data-stu-id="1dd15-125">The amount of data is small so that evaluating on the client doesn't incur a huge performance penalty.</span></span>
- <span data-ttu-id="1dd15-126">Kullanılan LINQ işlecinin sunucu tarafı çevirisi yok.</span><span class="sxs-lookup"><span data-stu-id="1dd15-126">The LINQ operator being used has no server-side translation.</span></span>

<span data-ttu-id="1dd15-127">Böyle durumlarda, `AsEnumerable` veya `ToList` (`AsAsyncEnumerable` veya `ToListAsync` zaman uyumsuz) gibi yöntemleri çağırarak istemci değerlendirmesini açıkça tercih edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dd15-127">In such cases, you can explicitly opt into client evaluation by calling methods like `AsEnumerable` or `ToList` (`AsAsyncEnumerable` or `ToListAsync` for async).</span></span> <span data-ttu-id="1dd15-128">@No__t kullanarak, sonuçları akışla, ancak `ToList` ' i kullanmak, ek bellek de alan bir liste oluşturarak arabelleğe alma işlemine neden olur.</span><span class="sxs-lookup"><span data-stu-id="1dd15-128">By using `AsEnumerable` you would be streaming the results, but using `ToList` would cause buffering by creating a list, which also takes additional memory.</span></span> <span data-ttu-id="1dd15-129">Birden çok kez numaralandırdıysanız, sonuçları bir listede depolamak, veritabanında yalnızca bir sorgu olduğundan daha fazlasına yardımcı olur.</span><span class="sxs-lookup"><span data-stu-id="1dd15-129">Though if you're enumerating multiple times, then storing results in a list helps more since there's only one query to the database.</span></span> <span data-ttu-id="1dd15-130">Belirli bir kullanıma bağlı olarak, hangi yöntemin durum için daha yararlı olduğunu değerlendirmeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-130">Depending on the particular usage, you should evaluate which method is more useful for the case.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ExplicitClientEval)]

## <a name="potential-memory-leak-in-client-evaluation"></a><span data-ttu-id="1dd15-131">İstemci değerlendirmesinde olası bellek sızıntısı</span><span class="sxs-lookup"><span data-stu-id="1dd15-131">Potential memory leak in client evaluation</span></span>

<span data-ttu-id="1dd15-132">Sorgu çevirisi ve derleme pahalı olduğundan, derlenmiş sorgu planını EF Core önbelleğe alır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-132">Since query translation and compilation are expensive, EF Core caches the compiled query plan.</span></span> <span data-ttu-id="1dd15-133">Önbelleğe alınan temsilci, en üst düzey projeksiyonun istemci değerlendirmesi sırasında istemci kodunu kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-133">The cached delegate may use client code while doing client evaluation of top-level projection.</span></span> <span data-ttu-id="1dd15-134">EF Core, ağacın istemci tarafından değerlendirilen parçaları için parametreler üretir ve parametre değerlerini değiştirerek sorgu planını yeniden kullanır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-134">EF Core generates parameters for the client-evaluated parts of the tree and reuses the query plan by replacing the parameter values.</span></span> <span data-ttu-id="1dd15-135">Ancak ifade ağacındaki bazı sabitler parametrelere dönüştürülemez.</span><span class="sxs-lookup"><span data-stu-id="1dd15-135">But certain constants in the expression tree can't be converted into parameters.</span></span> <span data-ttu-id="1dd15-136">Önbelleğe alınmış temsilci bu tür sabitleri içeriyorsa, bu nesneler hala başvurulduğu için atık olarak toplanamaz.</span><span class="sxs-lookup"><span data-stu-id="1dd15-136">If the cached delegate contains such constants, then those objects can't be garbage collected since they're still being referenced.</span></span> <span data-ttu-id="1dd15-137">Böyle bir nesne, içindeki bir DbContext veya diğer hizmetleri içeriyorsa, uygulamanın bellek kullanımının zaman içinde büyümesine neden olabilir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-137">If such an object contains a DbContext or other services in it, then it could cause the memory usage of the app to grow over time.</span></span> <span data-ttu-id="1dd15-138">Bu davranış genellikle Bellek sızıntısının bir imzadır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-138">This behavior is generally a sign of a memory leak.</span></span> <span data-ttu-id="1dd15-139">EF Core, geçerli veritabanı sağlayıcısı kullanılarak eşleştirilenemeyen bir türün sabitlerinde her geldiğinde bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="1dd15-139">EF Core throws an exception whenever it comes across constants of a type that can't be mapped using current database provider.</span></span> <span data-ttu-id="1dd15-140">Yaygın nedenler ve çözümleri aşağıdaki gibidir:</span><span class="sxs-lookup"><span data-stu-id="1dd15-140">Common causes and their solutions are as follows:</span></span>

- <span data-ttu-id="1dd15-141">**Örnek yöntemi kullanma**: Bir istemci projeksiyonundaki örnek yöntemleri kullanırken, ifade ağacı örneğin bir sabitini içerir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-141">**Using an instance method**: When using instance methods in a client projection, the expression tree contains a constant of the instance.</span></span> <span data-ttu-id="1dd15-142">Yönteminiz örnekten herhangi bir veri kullanmıyorsa, yöntemi statik hale getirmeyi düşünün.</span><span class="sxs-lookup"><span data-stu-id="1dd15-142">If your method doesn't use any data from the instance, consider making the method static.</span></span> <span data-ttu-id="1dd15-143">Yöntem gövdesinde örnek veriye ihtiyacınız varsa, belirli verileri yöntemine bir bağımsız değişken olarak geçirin.</span><span class="sxs-lookup"><span data-stu-id="1dd15-143">If you need instance data in the method body, then pass the specific data as an argument to the method.</span></span>
- <span data-ttu-id="1dd15-144">**Yönteme sabit bağımsız değişkenler geçiliyor**: Bu durum genellikle istemci yöntemine yönelik bir bağımsız değişkende `this` kullanılarak yapılır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-144">**Passing constant arguments to method**: This case arises generally by using `this` in an argument to client method.</span></span> <span data-ttu-id="1dd15-145">İçindeki bağımsız değişkenini, veritabanı sağlayıcısı tarafından eşlenmekte olan birden çok skaler bağımsız değişkene bölmeyi göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="1dd15-145">Consider splitting the argument in to multiple scalar arguments, which can be mapped by the database provider.</span></span>
- <span data-ttu-id="1dd15-146">**Diğer sabitler**: Başka herhangi bir durumda bir sabit geliyorsa, sabit işlemin işlenmek üzere gerekli olup olmadığını değerlendirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="1dd15-146">**Other constants**: If a constant is come across in any other case, then you can evaluate whether the constant is needed in processing.</span></span> <span data-ttu-id="1dd15-147">Sabit olması gerekiyorsa veya yukarıdaki durumlardan bir çözüm kullanamıyoruz, değeri depolamak için yerel bir değişken oluşturun ve sorguda yerel değişkeni kullanın.</span><span class="sxs-lookup"><span data-stu-id="1dd15-147">If it's necessary to have the constant, or if you can't use a solution from the above cases, then create a local variable to store the value and use local variable in the query.</span></span> <span data-ttu-id="1dd15-148">EF Core, yerel değişkeni bir parametreye dönüştürecek.</span><span class="sxs-lookup"><span data-stu-id="1dd15-148">EF Core will convert the local variable into a parameter.</span></span>

## <a name="previous-versions"></a><span data-ttu-id="1dd15-149">Önceki sürümler</span><span class="sxs-lookup"><span data-stu-id="1dd15-149">Previous versions</span></span>

<span data-ttu-id="1dd15-150">Aşağıdaki bölüm 3,0 öncesi EF Core sürümler için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-150">The following section applies to EF Core versions before 3.0.</span></span>

<span data-ttu-id="1dd15-151">Daha eski EF Core, bir sorgunun herhangi bir bölümünde istemci değerlendirmesi desteklenir; yalnızca üst düzey projeksiyon değil.</span><span class="sxs-lookup"><span data-stu-id="1dd15-151">Older EF Core versions supported client evaluation in any part of the query--not just the top-level projection.</span></span> <span data-ttu-id="1dd15-152">Bu nedenle, [Desteklenmeyen istemci değerlendirme](#unsupported-client-evaluation) bölümü altında gönderilen sorgular doğru bir şekilde çalışmıştır.</span><span class="sxs-lookup"><span data-stu-id="1dd15-152">That's why queries similar to one posted under the [Unsupported client evaluation](#unsupported-client-evaluation) section worked correctly.</span></span> <span data-ttu-id="1dd15-153">Bu davranış fark edilmemiş performans sorunlarına neden olabileceği için EF Core istemci değerlendirme uyarısını günlüğe kaydetti.</span><span class="sxs-lookup"><span data-stu-id="1dd15-153">Since this behavior could cause unnoticed performance issues, EF Core logged a client evaluation warning.</span></span> <span data-ttu-id="1dd15-154">Günlüğe kaydetme çıkışını görüntüleme hakkında daha fazla bilgi için bkz. [Logging](xref:core/miscellaneous/logging).</span><span class="sxs-lookup"><span data-stu-id="1dd15-154">For more information on viewing logging output, see [Logging](xref:core/miscellaneous/logging).</span></span>

<span data-ttu-id="1dd15-155">İsteğe bağlı olarak EF Core, varsayılan davranışı bir özel durum oluşturmak veya istemci değerlendirmesi yaparken (projeksiyonu hariç) hiçbir şey yapmak üzere değiştirmenize izin verilir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-155">Optionally EF Core allowed you to change the default behavior to either throw an exception or do nothing when doing client evaluation (except for in the projection).</span></span> <span data-ttu-id="1dd15-156">Özel durum atma davranışı, 3,0 'deki davranışa benzer hale gelir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-156">The exception throwing behavior would make it similar to the behavior in 3.0.</span></span> <span data-ttu-id="1dd15-157">Davranışı değiştirmek için, bağlam için seçenekleri ayarlarken (genellikle `DbContext.OnConfiguring` ' da) veya ASP.NET Core kullanıyorsanız, `Startup.cs` ' de uyarı yapılandırmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="1dd15-157">To change the behavior, you need to configure warnings while setting up the options for your context - typically in `DbContext.OnConfiguring`, or in `Startup.cs` if you're using ASP.NET Core.</span></span>

```csharp
protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
{
    optionsBuilder
        .UseSqlServer(@"Server=(localdb)\mssqllocaldb;Database=EFQuerying;Trusted_Connection=True;")
        .ConfigureWarnings(warnings => warnings.Throw(RelationalEventId.QueryClientEvaluationWarning));
}
```
