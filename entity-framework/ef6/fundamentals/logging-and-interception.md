---
title: Günlüğe kaydetme ve veritabanı işlemlerini kesintiye alma-EF6
author: divega
ms.date: 10/23/2016
ms.assetid: b5ee7eb1-88cc-456e-b53c-c67e24c3f8ca
ms.openlocfilehash: 35b0284a5ad8b2b732f074589bd458d243312575
ms.sourcegitcommit: cc0ff36e46e9ed3527638f7208000e8521faef2e
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78419480"
---
# <a name="logging-and-intercepting-database-operations"></a><span data-ttu-id="f5b45-102">Veritabanı işlemlerini günlüğe kaydetme ve önleme</span><span class="sxs-lookup"><span data-stu-id="f5b45-102">Logging and intercepting database operations</span></span>
> [!NOTE]
> <span data-ttu-id="f5b45-103">**Yalnızca EF6** , bu sayfada açıklanan özellikler, API 'ler, vb. Entity Framework 6 ' da sunulmuştur.</span><span class="sxs-lookup"><span data-stu-id="f5b45-103">**EF6 Onwards Only** - The features, APIs, etc. discussed in this page were introduced in Entity Framework 6.</span></span> <span data-ttu-id="f5b45-104">Önceki bir sürümü kullanıyorsanız, bilgilerin bazıları veya tümü uygulanmaz.</span><span class="sxs-lookup"><span data-stu-id="f5b45-104">If you are using an earlier version, some or all of the information does not apply.</span></span>  

<span data-ttu-id="f5b45-105">Entity Framework 6 ' dan başlayarak, her zaman Entity Framework veritabanına bir komut gönderir bu komut uygulama kodu tarafından yakalanabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-105">Starting with Entity Framework 6, anytime Entity Framework sends a command to the database this command can be intercepted by application code.</span></span> <span data-ttu-id="f5b45-106">Bu, yaygın olarak SQL günlüğü için kullanılır, ancak komutu değiştirmek veya durdurmak için de kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-106">This is most commonly used for logging SQL, but can also be used to modify or abort the command.</span></span>  

<span data-ttu-id="f5b45-107">Özel olarak, EF şunları içerir:</span><span class="sxs-lookup"><span data-stu-id="f5b45-107">Specifically, EF includes:</span></span>  

- <span data-ttu-id="f5b45-108">LINQ to SQL DataContext. log dosyasına benzer bağlam için bir günlük özelliği</span><span class="sxs-lookup"><span data-stu-id="f5b45-108">A Log property for the context similar to DataContext.Log in LINQ to SQL</span></span>  
- <span data-ttu-id="f5b45-109">Günlüğe gönderilen çıktının içeriğini ve biçimlendirmesini özelleştirmek için bir mekanizma</span><span class="sxs-lookup"><span data-stu-id="f5b45-109">A mechanism to customize the content and formatting of the output sent to the log</span></span>  
- <span data-ttu-id="f5b45-110">Daha fazla denetim/esneklik sağlayan, ele geçirme için düşük düzey yapı taşları</span><span class="sxs-lookup"><span data-stu-id="f5b45-110">Low-level building blocks for interception giving greater control/flexibility</span></span>  

## <a name="context-log-property"></a><span data-ttu-id="f5b45-111">Bağlam günlüğü özelliği</span><span class="sxs-lookup"><span data-stu-id="f5b45-111">Context Log property</span></span>  

<span data-ttu-id="f5b45-112">DbContext. Database. Log özelliği, bir dize alan herhangi bir yöntem için bir temsilciye ayarlanabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-112">The DbContext.Database.Log property can be set to a delegate for any method that takes a string.</span></span> <span data-ttu-id="f5b45-113">En yaygın olarak, bu TextWriter 'ın "yazma" yöntemine ayarlanarak herhangi bir TextWriter ile birlikte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-113">Most commonly it is used with any TextWriter by setting it to the “Write” method of that TextWriter.</span></span> <span data-ttu-id="f5b45-114">Geçerli bağlam tarafından oluşturulan tüm SQL bu yazıcıya kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-114">All SQL generated by the current context will be logged to that writer.</span></span> <span data-ttu-id="f5b45-115">Örneğin, aşağıdaki kod SQL 'i konsola kaydeder:</span><span class="sxs-lookup"><span data-stu-id="f5b45-115">For example, the following code will log SQL to the console:</span></span>  

``` csharp
using (var context = new BlogContext())
{
    context.Database.Log = Console.Write;

    // Your code here...
}
```  

<span data-ttu-id="f5b45-116">Bağlam olduğuna dikkat edin. Database. log, Console. Write olarak ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-116">Notice that context.Database.Log is set to Console.Write.</span></span> <span data-ttu-id="f5b45-117">Bu, SQL 'i konsolunda günlüğe kaydetmek için gereklidir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-117">This is all that is needed to log SQL to the console.</span></span>  

<span data-ttu-id="f5b45-118">Bazı çıktıları görebilmemiz için bazı basit sorgu/ekleme/güncelleştirme kodu ekleyelim:</span><span class="sxs-lookup"><span data-stu-id="f5b45-118">Let’s add some simple query/insert/update code so that we can see some output:</span></span>  

``` csharp
using (var context = new BlogContext())
{
    context.Database.Log = Console.Write;

    var blog = context.Blogs.First(b => b.Title == "One Unicorn");

    blog.Posts.First().Title = "Green Eggs and Ham";

    blog.Posts.Add(new Post { Title = "I do not like them!" });

    context.SaveChangesAsync().Wait();
}
```  

<span data-ttu-id="f5b45-119">Bu, aşağıdaki çıktıyı oluşturur:</span><span class="sxs-lookup"><span data-stu-id="f5b45-119">This will generate the following output:</span></span>  

``` SQL
SELECT TOP (1)
    [Extent1].[Id] AS [Id],
    [Extent1].[Title] AS [Title]
    FROM [dbo].[Blogs] AS [Extent1]
    WHERE (N'One Unicorn' = [Extent1].[Title]) AND ([Extent1].[Title] IS NOT NULL)
-- Executing at 10/8/2013 10:55:41 AM -07:00
-- Completed in 4 ms with result: SqlDataReader

SELECT
    [Extent1].[Id] AS [Id],
    [Extent1].[Title] AS [Title],
    [Extent1].[BlogId] AS [BlogId]
    FROM [dbo].[Posts] AS [Extent1]
    WHERE [Extent1].[BlogId] = @EntityKeyValue1
-- EntityKeyValue1: '1' (Type = Int32)
-- Executing at 10/8/2013 10:55:41 AM -07:00
-- Completed in 2 ms with result: SqlDataReader

UPDATE [dbo].[Posts]
SET [Title] = @0
WHERE ([Id] = @1)
-- @0: 'Green Eggs and Ham' (Type = String, Size = -1)
-- @1: '1' (Type = Int32)
-- Executing asynchronously at 10/8/2013 10:55:41 AM -07:00
-- Completed in 12 ms with result: 1

INSERT [dbo].[Posts]([Title], [BlogId])
VALUES (@0, @1)
SELECT [Id]
FROM [dbo].[Posts]
WHERE @@ROWCOUNT > 0 AND [Id] = scope_identity()
-- @0: 'I do not like them!' (Type = String, Size = -1)
-- @1: '1' (Type = Int32)
-- Executing asynchronously at 10/8/2013 10:55:41 AM -07:00
-- Completed in 2 ms with result: SqlDataReader
```  

<span data-ttu-id="f5b45-120">(Bu, herhangi bir veritabanı başlatmasının zaten gerçekleştiğini kabul eden çıkış olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-120">(Note that this is the output assuming any database initialization has already happened.</span></span> <span data-ttu-id="f5b45-121">Veritabanı başlatma işlemi henüz gerçekleşmiyorsa, tüm çalışma geçişlerinin, yeni bir veritabanını denetlemeye veya oluşturmaya yönelik kapakların altında olduğunu gösteren çok daha fazla çıkış olacaktır.)</span><span class="sxs-lookup"><span data-stu-id="f5b45-121">If database initialization had not already happened then there would be a lot more output showing all the work Migrations does under the covers to check for or create a new database.)</span></span>  

## <a name="what-gets-logged"></a><span data-ttu-id="f5b45-122">Ne günlüğe kaydedilir?</span><span class="sxs-lookup"><span data-stu-id="f5b45-122">What gets logged?</span></span>  

<span data-ttu-id="f5b45-123">Log özelliği ayarlandığında, aşağıdakilerin tümü günlüğe kaydedilir:</span><span class="sxs-lookup"><span data-stu-id="f5b45-123">When the Log property is set all of the following will be logged:</span></span>  

- <span data-ttu-id="f5b45-124">Tüm farklı komut türleri için SQL.</span><span class="sxs-lookup"><span data-stu-id="f5b45-124">SQL for all different kinds of commands.</span></span> <span data-ttu-id="f5b45-125">Örnek:</span><span class="sxs-lookup"><span data-stu-id="f5b45-125">For example:</span></span>  
    - <span data-ttu-id="f5b45-126">Normal LINQ sorguları, eSQL sorguları ve SqlQuery gibi yöntemlerden ham sorgular da dahil olmak üzere sorgular</span><span class="sxs-lookup"><span data-stu-id="f5b45-126">Queries, including normal LINQ queries, eSQL queries, and raw queries from methods such as SqlQuery</span></span>  
    - <span data-ttu-id="f5b45-127">SaveChanges 'un bir parçası olarak oluşturulan ekler, güncelleştirmeler ve siler</span><span class="sxs-lookup"><span data-stu-id="f5b45-127">Inserts, updates, and deletes generated as part of SaveChanges</span></span>  
    - <span data-ttu-id="f5b45-128">Geç yükleme tarafından oluşturulanlar gibi ilişki yükleme sorguları</span><span class="sxs-lookup"><span data-stu-id="f5b45-128">Relationship loading queries such as those generated by lazy loading</span></span>  
- <span data-ttu-id="f5b45-129">Parametreler</span><span class="sxs-lookup"><span data-stu-id="f5b45-129">Parameters</span></span>  
- <span data-ttu-id="f5b45-130">Komutun zaman uyumsuz olarak yürütülüp yürütülmediğini belirtir</span><span class="sxs-lookup"><span data-stu-id="f5b45-130">Whether or not the command is being executed asynchronously</span></span>  
- <span data-ttu-id="f5b45-131">Komutun ne zaman yürütülmeye başlatıldığını belirten bir zaman damgası</span><span class="sxs-lookup"><span data-stu-id="f5b45-131">A timestamp indicating when the command started executing</span></span>  
- <span data-ttu-id="f5b45-132">Komutun başarıyla tamamlanıp tamamlanmayacağı, bir özel durum oluşturan veya zaman uyumsuz olarak iptal edildiği</span><span class="sxs-lookup"><span data-stu-id="f5b45-132">Whether or not the command completed successfully, failed by throwing an exception, or, for async, was canceled</span></span>  
- <span data-ttu-id="f5b45-133">Sonuç değerinin bir göstergesi</span><span class="sxs-lookup"><span data-stu-id="f5b45-133">Some indication of the result value</span></span>  
- <span data-ttu-id="f5b45-134">Komutu yürütmek için geçen sürenin yaklaşık miktarı.</span><span class="sxs-lookup"><span data-stu-id="f5b45-134">The approximate amount of time it took to execute the command.</span></span> <span data-ttu-id="f5b45-135">Bu, sonuç nesnesini geri almak için komutunu göndermenin zamanı olduğunu unutmayın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-135">Note that this is the time from sending the command to getting the result object back.</span></span> <span data-ttu-id="f5b45-136">Sonuçların okunması zaman içermez.</span><span class="sxs-lookup"><span data-stu-id="f5b45-136">It does not include time to read the results.</span></span>  

<span data-ttu-id="f5b45-137">Yukarıdaki örnek çıktıya bakarak günlüğe yazılan dört komutun her biri şu şekilde yapılır:</span><span class="sxs-lookup"><span data-stu-id="f5b45-137">Looking at the example output above, each of the four commands logged are:</span></span>  

- <span data-ttu-id="f5b45-138">Sorgu bağlama çağrısının sonucu. Ilk blog.</span><span class="sxs-lookup"><span data-stu-id="f5b45-138">The query resulting from the call to context.Blogs.First</span></span>  
    - <span data-ttu-id="f5b45-139">"First", ToString 'in çağrılabilecek bir IQueryable sağlamamasından bu yana, SQL 'i alma ToString yönteminin bu sorgu için çalışmadığına dikkat edin</span><span class="sxs-lookup"><span data-stu-id="f5b45-139">Notice that the ToString method of getting the SQL would not have worked for this query since “First” does not provide an IQueryable on which ToString could be called</span></span>  
- <span data-ttu-id="f5b45-140">Sorgu, blogun geç yükleme işleminden elde edilir. Nakil</span><span class="sxs-lookup"><span data-stu-id="f5b45-140">The query resulting from the lazy-loading of blog.Posts</span></span>  
    - <span data-ttu-id="f5b45-141">Yavaş yüklemenin gerçekleştiği anahtar değer için parametre ayrıntılarına dikkat edin</span><span class="sxs-lookup"><span data-stu-id="f5b45-141">Notice the parameter details for the key value for which lazy loading is happening</span></span>  
    - <span data-ttu-id="f5b45-142">Yalnızca varsayılan olmayan değerlere ayarlanan parametrelerin özellikleri günlüğe kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-142">Only properties of the parameter that are set to non-default values are logged.</span></span> <span data-ttu-id="f5b45-143">Örneğin, size özelliği yalnızca sıfır dışında bir değer olarak gösterilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-143">For example, the Size property is only shown if it is non-zero.</span></span>  
- <span data-ttu-id="f5b45-144">SaveChangesAsync 'den kaynaklanan iki komut Bunlardan biri, bir posta başlığını değiştirmek için bir INSERT, diğeri yeni gönderi eklemek için</span><span class="sxs-lookup"><span data-stu-id="f5b45-144">Two commands resulting from SaveChangesAsync; one for the update to change a post title, the other for an insert to add a new post</span></span>  
    - <span data-ttu-id="f5b45-145">FK ve title özelliklerine ilişkin parametre ayrıntılarına dikkat edin</span><span class="sxs-lookup"><span data-stu-id="f5b45-145">Notice the parameter details for the FK and Title properties</span></span>  
    - <span data-ttu-id="f5b45-146">Bu komutların zaman uyumsuz olarak yürütülebileceğini unutmayın</span><span class="sxs-lookup"><span data-stu-id="f5b45-146">Notice that these commands are being executed asynchronously</span></span>  

## <a name="logging-to-different-places"></a><span data-ttu-id="f5b45-147">Farklı yerlere kaydetme</span><span class="sxs-lookup"><span data-stu-id="f5b45-147">Logging to different places</span></span>  

<span data-ttu-id="f5b45-148">Yukarıdaki konsolda gösterildiği gibi, daha kolay bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-148">As shown above logging to the console is super easy.</span></span> <span data-ttu-id="f5b45-149">Ayrıca, farklı tür [TextWriter](https://msdn.microsoft.com/library/system.io.textwriter.aspx)kullanarak bellek, dosya vb. günlük kaydı da kolaydır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-149">It’s also easy to log to memory, file, etc. by using different kinds of [TextWriter](https://msdn.microsoft.com/library/system.io.textwriter.aspx).</span></span>  

<span data-ttu-id="f5b45-150">LINQ to SQL biliyorsanız, log özelliğinin gerçek TextWriter nesnesine (örneğin, Console. out) ayarlandığını, EF 'teki log özelliğinin ise bir dizeyi kabul eden bir yönteme ayarlandığını LINQ to SQL fark edebilirsiniz (örneğin, , Console. Write veya Console. out. Write).</span><span class="sxs-lookup"><span data-stu-id="f5b45-150">If you are familiar with LINQ to SQL you might notice that in LINQ to SQL the Log property is set to the actual TextWriter object (for example, Console.Out) while in EF the Log property is set to a method that accepts a string (for example, Console.Write or Console.Out.Write).</span></span> <span data-ttu-id="f5b45-151">Bunun nedeni, dizeler için havuz görevi gören herhangi bir temsilciyi kabul ederek TextWriter 'dan EF 'e ayrılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-151">The reason for this is to decouple EF from TextWriter by accepting any delegate that can act as a sink for strings.</span></span> <span data-ttu-id="f5b45-152">Örneğin, bir günlük çerçevesini zaten kullandığınızı ve bunun gibi bir günlüğe kaydetme yöntemi tanımladığınızı düşünün:</span><span class="sxs-lookup"><span data-stu-id="f5b45-152">For example, imagine that you already have some logging framework and it defines a logging method like so:</span></span>  

``` csharp
public class MyLogger
{
    public void Log(string component, string message)
    {
        Console.WriteLine("Component: {0} Message: {1} ", component, message);
    }
}
```  

<span data-ttu-id="f5b45-153">Bu, EF Log özelliğine şu şekilde erişilebilir:</span><span class="sxs-lookup"><span data-stu-id="f5b45-153">This could be hooked up to the EF Log property like this:</span></span>  

``` csharp
var logger = new MyLogger();
context.Database.Log = s => logger.Log("EFApp", s);
```  

## <a name="result-logging"></a><span data-ttu-id="f5b45-154">Sonuç günlüğü</span><span class="sxs-lookup"><span data-stu-id="f5b45-154">Result logging</span></span>  

<span data-ttu-id="f5b45-155">Varsayılan günlükçü komut metnini (SQL), parametreleri ve "yürütülüyor" satırını komut veritabanına gönderilmeden önce bir zaman damgasıyla günlüğe kaydeder.</span><span class="sxs-lookup"><span data-stu-id="f5b45-155">The default logger logs command text (SQL), parameters, and the “Executing” line with a timestamp before the command is sent to the database.</span></span> <span data-ttu-id="f5b45-156">Geçen süreyi içeren "tamamlandı" satırı komutun yürütülmesi için günlüğe kaydedilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-156">A “completed” line containing elapsed time is logged following execution of the command.</span></span>  

<span data-ttu-id="f5b45-157">Zaman uyumsuz komutlar için "tamamlandı" satırının, zaman uyumsuz görev gerçekten tamamlanana, başarısız olmasına veya iptal edilene kadar günlüğe kaydedilmeyeceğini unutmayın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-157">Note that for async commands the “completed” line is not logged until the async task actually completes, fails, or is canceled.</span></span>  

<span data-ttu-id="f5b45-158">"Tamamlandı" satırı, komutun türüne ve yürütmenin başarılı olup olmadığına bağlı olarak farklı bilgiler içerir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-158">The “completed” line contains different information depending on the type of command and whether or not execution was successful.</span></span>  

### <a name="successful-execution"></a><span data-ttu-id="f5b45-159">Başarılı yürütme</span><span class="sxs-lookup"><span data-stu-id="f5b45-159">Successful execution</span></span>  

<span data-ttu-id="f5b45-160">Başarılı bir şekilde tamamlanan komutlar için, çıkış "x MS 'de şu sonuçla tamamlandı:" sözcüğünün ardından sonucun ne olduğuna ilişkin bir bildirim gelir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-160">For commands that complete successfully the output is “Completed in x ms with result: “ followed by some indication of what the result was.</span></span> <span data-ttu-id="f5b45-161">Bir veri okuyucusu döndüren komutlar için sonuç göstergesi döndürülen [DbDataReader](https://msdn.microsoft.com/library/system.data.common.dbdatareader.aspx) türüdür.</span><span class="sxs-lookup"><span data-stu-id="f5b45-161">For commands that return a data reader the result indication is the type of [DbDataReader](https://msdn.microsoft.com/library/system.data.common.dbdatareader.aspx) returned.</span></span> <span data-ttu-id="f5b45-162">Gösterilen sonucun yukarıda gösterilen Update komutu gibi bir tamsayı değer döndüren komutlar için bu tamsayıdır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-162">For commands that return an integer value such as the update command shown above the result shown is that integer.</span></span>  

### <a name="failed-execution"></a><span data-ttu-id="f5b45-163">Yürütme başarısız oldu</span><span class="sxs-lookup"><span data-stu-id="f5b45-163">Failed execution</span></span>  

<span data-ttu-id="f5b45-164">Özel durum oluşturan komutlar için, çıkış özel durumdan gelen iletiyi içerir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-164">For commands that fail by throwing an exception, the output contains the message from the exception.</span></span> <span data-ttu-id="f5b45-165">Örneğin, var olan bir tabloya karşı sorgulamak için SqlQuery kullanmak şuna benzer bir şekilde günlük çıkışı oluşmasına neden olur:</span><span class="sxs-lookup"><span data-stu-id="f5b45-165">For example, using SqlQuery to query against a table that does exist will result in log output something like this:</span></span>  

``` SQL
SELECT * from ThisTableIsMissing
-- Executing at 5/13/2013 10:19:05 AM
-- Failed in 1 ms with error: Invalid object name 'ThisTableIsMissing'.
```  

### <a name="canceled-execution"></a><span data-ttu-id="f5b45-166">Yürütme iptal edildi</span><span class="sxs-lookup"><span data-stu-id="f5b45-166">Canceled execution</span></span>  

<span data-ttu-id="f5b45-167">Görevin iptal edildiği zaman uyumsuz komutlar için, bu, temel alınan ADO.NET sağlayıcının genellikle iptal için bir deneme yapıldığında yaptığı durumlar olduğundan, bir özel durumla başarısız olabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-167">For async commands where the task is canceled the result could be failure with an exception, since this is what the underlying ADO.NET provider often does when an attempt is made to cancel.</span></span> <span data-ttu-id="f5b45-168">Bu durum gerçekleşmezse ve görev düzgün şekilde iptal edilirse, çıkış şuna benzer şekilde görünür:</span><span class="sxs-lookup"><span data-stu-id="f5b45-168">If this doesn’t happen and the task is canceled cleanly then the output will look something like this:</span></span>  

```console
update Blogs set Title = 'No' where Id = -1
-- Executing asynchronously at 5/13/2013 10:21:10 AM
-- Canceled in 1 ms
```  

## <a name="changing-log-content-and-formatting"></a><span data-ttu-id="f5b45-169">Günlük içeriğini ve biçimlendirmeyi değiştirme</span><span class="sxs-lookup"><span data-stu-id="f5b45-169">Changing log content and formatting</span></span>  

<span data-ttu-id="f5b45-170">Öğesinin altında Database. Log özelliği bir DatabaseLogFormatter nesnesinin kullanımını sağlar.</span><span class="sxs-lookup"><span data-stu-id="f5b45-170">Under the covers the Database.Log property makes use of a DatabaseLogFormatter object.</span></span> <span data-ttu-id="f5b45-171">Bu nesne, dize ve DbContext kabul eden bir temsilciye bir ıdbcommandyakalayıcısı uygulamasını (aşağıya bakın) etkin bir şekilde bağlar.</span><span class="sxs-lookup"><span data-stu-id="f5b45-171">This object effectively binds an IDbCommandInterceptor implementation (see below) to a delegate that accepts strings and a DbContext.</span></span> <span data-ttu-id="f5b45-172">Bu, DatabaseLogFormatter üzerindeki yöntemlerin, komutların EF tarafından yürütülmesinden önce ve sonra çağrıldığı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-172">This means that methods on DatabaseLogFormatter are called before and after the execution of commands by EF.</span></span> <span data-ttu-id="f5b45-173">Bu DatabaseLogFormatter yöntemleri, günlük çıktısını toplayıp biçimlendirir ve temsilciye gönderir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-173">These DatabaseLogFormatter methods gather and format log output and send it to the delegate.</span></span>  

### <a name="customizing-databaselogformatter"></a><span data-ttu-id="f5b45-174">DatabaseLogFormatter 'ı özelleştirme</span><span class="sxs-lookup"><span data-stu-id="f5b45-174">Customizing DatabaseLogFormatter</span></span>  

<span data-ttu-id="f5b45-175">Günlüğe kaydedilen ve nasıl biçimlendirildiğine yönelik değişiklik, DatabaseLogFormatter 'dan türeyen yeni bir sınıf oluşturularak ve uygun şekilde Yöntemler geçersiz kılınarak elde edilebilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-175">Changing what is logged and how it is formatted can be achieved by creating a new class that derives from DatabaseLogFormatter and overrides methods as appropriate.</span></span> <span data-ttu-id="f5b45-176">Geçersiz kılınacak en yaygın yöntemler şunlardır:</span><span class="sxs-lookup"><span data-stu-id="f5b45-176">The most common methods to override are:</span></span>  

- <span data-ttu-id="f5b45-177">LogCommand: komutları yürütülmeden önce günlüğe kaydetme şeklini değiştirmek için bunu geçersiz kılın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-177">LogCommand – Override this to change how commands are logged before they are executed.</span></span> <span data-ttu-id="f5b45-178">Varsayılan olarak, LogCommand her parametre için LogParameter çağırır; Bunun yerine, geçersiz kılmanızda veya parametreleri farklı şekilde işleyebilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-178">By default LogCommand calls LogParameter for each parameter; you may choose to do the same in your override or handle parameters differently instead.</span></span>  
- <span data-ttu-id="f5b45-179">LogResult: bir komutu yürütme sonucunun günlüğe nasıl kaydedileceğini değiştirmek için bunu geçersiz kılın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-179">LogResult – Override this to change how the outcome from executing a command is logged.</span></span>  
- <span data-ttu-id="f5b45-180">LogParameter: parametre günlüğünün biçimlendirmesini ve içeriğini değiştirmek için bunu geçersiz kılın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-180">LogParameter – Override this to change the formatting and content of parameter logging.</span></span>  

<span data-ttu-id="f5b45-181">Örneğin, her komut veritabanına gönderilmeden önce yalnızca tek bir satırı günlüğe kaydetmek istediğinizi varsayalım.</span><span class="sxs-lookup"><span data-stu-id="f5b45-181">For example, suppose we wanted to log just a single line before each command is sent to the database.</span></span> <span data-ttu-id="f5b45-182">Bu, iki geçersiz kılma ile yapılabilir:</span><span class="sxs-lookup"><span data-stu-id="f5b45-182">This can be done with two overrides:</span></span>  

- <span data-ttu-id="f5b45-183">Tek SQL satırını biçimlendirmek ve yazmak için LogCommand 'i geçersiz kıl</span><span class="sxs-lookup"><span data-stu-id="f5b45-183">Override LogCommand to format and write the single line of SQL</span></span>  
- <span data-ttu-id="f5b45-184">Hiçbir şey yapmak için LogResult 'ı geçersiz kılın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-184">Override LogResult to do nothing.</span></span>  

<span data-ttu-id="f5b45-185">Kod şuna benzer:</span><span class="sxs-lookup"><span data-stu-id="f5b45-185">The code would look something like this:</span></span>

``` csharp
public class OneLineFormatter : DatabaseLogFormatter
{
    public OneLineFormatter(DbContext context, Action<string> writeAction)
        : base(context, writeAction)
    {
    }

    public override void LogCommand<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        Write(string.Format(
            "Context '{0}' is executing command '{1}'{2}",
            Context.GetType().Name,
            command.CommandText.Replace(Environment.NewLine, ""),
            Environment.NewLine));
    }

    public override void LogResult<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
    }
}
```  

<span data-ttu-id="f5b45-186">Çıktıyı günlüğe kaydetmek için, yapılandırılan yazma temsilcisine çıktı gönderecek olan Write metodunu çağırın.</span><span class="sxs-lookup"><span data-stu-id="f5b45-186">To log output simply call the Write method which will send output to the configured write delegate.</span></span>  

<span data-ttu-id="f5b45-187">(Bu kodun bir örnek olarak satır sonlarının uyarlaması kaldırdığına göz önünde kısınız.</span><span class="sxs-lookup"><span data-stu-id="f5b45-187">(Note that this code does simplistic removal of line breaks just as an example.</span></span> <span data-ttu-id="f5b45-188">Büyük olasılıkla karmaşık SQL görüntülemek için iyi çalışmaz.)</span><span class="sxs-lookup"><span data-stu-id="f5b45-188">It will likely not work well for viewing complex SQL.)</span></span>  

### <a name="setting-the-databaselogformatter"></a><span data-ttu-id="f5b45-189">DatabaseLogFormatter ayarlanıyor</span><span class="sxs-lookup"><span data-stu-id="f5b45-189">Setting the DatabaseLogFormatter</span></span>  

<span data-ttu-id="f5b45-190">Yeni bir DatabaseLogFormatter sınıfı oluşturulduktan sonra, EF ile kaydedilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-190">Once a new DatabaseLogFormatter class has been created it needs to be registered with EF.</span></span> <span data-ttu-id="f5b45-191">Bu, kod tabanlı yapılandırma kullanılarak yapılır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-191">This is done using code-based configuration.</span></span> <span data-ttu-id="f5b45-192">Bir kısaca 'de bu, DbContext sınıfınız ile aynı derlemede bulunan DBConfiguration 'dan türetilmiş yeni bir sınıf oluşturma ve ardından bu yeni sınıfın oluşturucusunda setdatabaselogformatter çağırma anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-192">In a nutshell this means creating a new class that derives from DbConfiguration in the same assembly as your DbContext class and then calling SetDatabaseLogFormatter in the constructor of this new class.</span></span> <span data-ttu-id="f5b45-193">Örnek:</span><span class="sxs-lookup"><span data-stu-id="f5b45-193">For example:</span></span>  

``` csharp
public class MyDbConfiguration : DbConfiguration
{
    public MyDbConfiguration()
    {
        SetDatabaseLogFormatter(
            (context, writeAction) => new OneLineFormatter(context, writeAction));
    }
}
```  

### <a name="using-the-new-databaselogformatter"></a><span data-ttu-id="f5b45-194">Yeni DatabaseLogFormatter 'ı kullanma</span><span class="sxs-lookup"><span data-stu-id="f5b45-194">Using the new DatabaseLogFormatter</span></span>  

<span data-ttu-id="f5b45-195">Bu yeni DatabaseLogFormatter artık her zaman veritabanı için kullanılacaktır. log ayarlandı.</span><span class="sxs-lookup"><span data-stu-id="f5b45-195">This new DatabaseLogFormatter will now be used anytime Database.Log is set.</span></span> <span data-ttu-id="f5b45-196">Bu nedenle, kod 1 ' den kodu çalıştırmak şu çıkışa neden olur:</span><span class="sxs-lookup"><span data-stu-id="f5b45-196">So, running the code from part 1 will now result in the following output:</span></span>  

```console
Context 'BlogContext' is executing command 'SELECT TOP (1) [Extent1].[Id] AS [Id], [Extent1].[Title] AS [Title]FROM [dbo].[Blogs] AS [Extent1]WHERE (N'One Unicorn' = [Extent1].[Title]) AND ([Extent1].[Title] IS NOT NULL)'
Context 'BlogContext' is executing command 'SELECT [Extent1].[Id] AS [Id], [Extent1].[Title] AS [Title], [Extent1].[BlogId] AS [BlogId]FROM [dbo].[Posts] AS [Extent1]WHERE [Extent1].[BlogId] = @EntityKeyValue1'
Context 'BlogContext' is executing command 'update [dbo].[Posts]set [Title] = @0where ([Id] = @1)'
Context 'BlogContext' is executing command 'insert [dbo].[Posts]([Title], [BlogId])values (@0, @1)select [Id]from [dbo].[Posts]where @@rowcount > 0 and [Id] = scope_identity()'
```  

## <a name="interception-building-blocks"></a><span data-ttu-id="f5b45-197">Yakalaşmayı oluşturma blokları</span><span class="sxs-lookup"><span data-stu-id="f5b45-197">Interception building blocks</span></span>  

<span data-ttu-id="f5b45-198">Şu ana kadar, EF tarafından oluşturulan SQL 'i günlüğe kaydetmek için DbContext. Database. log ' un nasıl kullanılacağını inceledik.</span><span class="sxs-lookup"><span data-stu-id="f5b45-198">So far we have looked at how to use DbContext.Database.Log to log the SQL generated by EF.</span></span> <span data-ttu-id="f5b45-199">Ancak bu kod, daha genel bir ele alma için bazı düşük düzeyli yapı taşları üzerinde görece ince façlşdir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-199">But this code is actually a relatively thin facade over some low-level building blocks for more general interception.</span></span>  

### <a name="interception-interfaces"></a><span data-ttu-id="f5b45-200">Araya geçirme arabirimleri</span><span class="sxs-lookup"><span data-stu-id="f5b45-200">Interception interfaces</span></span>  

<span data-ttu-id="f5b45-201">Yakasyon kodu, ele geçirme arabirimleri kavramı etrafında oluşturulmuştur.</span><span class="sxs-lookup"><span data-stu-id="f5b45-201">The interception code is built around the concept of interception interfaces.</span></span> <span data-ttu-id="f5b45-202">Bu arabirimler, ıdbyakalayıcıyı sınıfından devralınır ve EF bir işlem gerçekleştirdiğinde çağrılan yöntemleri tanımlar.</span><span class="sxs-lookup"><span data-stu-id="f5b45-202">These interfaces inherit from IDbInterceptor and define methods that are called when EF performs some action.</span></span> <span data-ttu-id="f5b45-203">Amaç, her nesne türü için bir arabirime sahip olmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-203">The intent is to have one interface per type of object being intercepted.</span></span> <span data-ttu-id="f5b45-204">Örneğin, ıdbcommandyakalayıcısı arabirimi, bir ExecuteNonQuery, ExecuteReader metodunu, ExecuteReader ve ilgili yöntemlere çağrı yapmadan önce çağrılan yöntemleri tanımlar.</span><span class="sxs-lookup"><span data-stu-id="f5b45-204">For example, the IDbCommandInterceptor interface defines methods that are called before EF makes a call to ExecuteNonQuery, ExecuteScalar, ExecuteReader, and related methods.</span></span> <span data-ttu-id="f5b45-205">Benzer şekilde, arabirim, bu işlemlerin her biri tamamlandığında çağrılan yöntemleri tanımlar.</span><span class="sxs-lookup"><span data-stu-id="f5b45-205">Likewise, the interface defines methods that are called when each of these operations completes.</span></span> <span data-ttu-id="f5b45-206">Yukarıda incelediğimiz DatabaseLogFormatter sınıfı bu arabirimi günlüğe kaydetmek için uygular.</span><span class="sxs-lookup"><span data-stu-id="f5b45-206">The DatabaseLogFormatter class that we looked at above implements this interface to log commands.</span></span>  

### <a name="the-interception-context"></a><span data-ttu-id="f5b45-207">Yakalenme bağlamı</span><span class="sxs-lookup"><span data-stu-id="f5b45-207">The interception context</span></span>  

<span data-ttu-id="f5b45-208">Herhangi bir dinleyici için tanımlanan yöntemlere bakarak, her çağrıya Dbyakationcontext türünde bir nesne veya Dbcommandyakationcontext\<\>gibi bazı tür bir nesne verilmediği görünür.</span><span class="sxs-lookup"><span data-stu-id="f5b45-208">Looking at the methods defined on any of the interceptor interfaces it is apparent that every call is given an object of type DbInterceptionContext or some type derived from this such as DbCommandInterceptionContext\<\>.</span></span> <span data-ttu-id="f5b45-209">Bu nesne, EF 'in aldığı eylem hakkında bağlamsal bilgiler içerir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-209">This object contains contextual information about the action that EF is taking.</span></span> <span data-ttu-id="f5b45-210">Örneğin, eylem bir DbContext adına götürülüiyorsa DbContext Dbyakationcontext içine dahil edilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-210">For example, if the action is being taken on behalf of a DbContext, then the DbContext is included in the DbInterceptionContext.</span></span> <span data-ttu-id="f5b45-211">Benzer şekilde, zaman uyumsuz olarak yürütülen komutlar için, Dbcommandyakationcontext üzerinde IsAsync bayrağı ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-211">Similarly, for commands that are being executed asynchronously, the IsAsync flag is set on DbCommandInterceptionContext.</span></span>  

### <a name="result-handling"></a><span data-ttu-id="f5b45-212">Sonuç işleme</span><span class="sxs-lookup"><span data-stu-id="f5b45-212">Result handling</span></span>  

<span data-ttu-id="f5b45-213">Dbcommandyakationcontext\<\> sınıfı result, OriginalResult, Exception ve OriginalException adlı bir özellik içerir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-213">The DbCommandInterceptionContext\<\> class contains a properties called Result, OriginalResult, Exception, and OriginalException.</span></span> <span data-ttu-id="f5b45-214">Bu özellikler, işlem yürütülmeden önce çağrılan çağrı yöntemlerine yapılan çağrılar için null/sıfır olarak ayarlanır; Yani,.................. Yöntemler yürütülüyor.</span><span class="sxs-lookup"><span data-stu-id="f5b45-214">These properties are set to null/zero for calls to the interception methods that are called before the operation is executed — that is, for the …Executing methods.</span></span> <span data-ttu-id="f5b45-215">İşlem yürütülürse ve başarılı olursa Result ve OriginalResult işlemin sonucuna ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-215">If the operation is executed and succeeds, then Result and OriginalResult are set to the result of the operation.</span></span> <span data-ttu-id="f5b45-216">Bu değerler daha sonra, işlem yürütüldükten sonra çağrılan (...... Çalıştırılan Yöntemler.</span><span class="sxs-lookup"><span data-stu-id="f5b45-216">These values can then be observed in the interception methods that are called after the operation has executed — that is, on the …Executed methods.</span></span> <span data-ttu-id="f5b45-217">Benzer şekilde, işlem oluşturursa, Exception ve OriginalException özellikleri ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-217">Likewise, if the operation throws, then the Exception and OriginalException properties will be set.</span></span>  

#### <a name="suppressing-execution"></a><span data-ttu-id="f5b45-218">Yürütmeyi gizleme</span><span class="sxs-lookup"><span data-stu-id="f5b45-218">Suppressing execution</span></span>  

<span data-ttu-id="f5b45-219">Bir dinleyici, komut yürütülmeden önce sonuç özelliğini ayarlarsa (bir... Yöntemler yürütülüyor), AŞV gerçekten komutu yürütmeye çalışmaz, ancak bunun yerine yalnızca sonuç kümesini kullanacaktır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-219">If an interceptor sets the Result property before the command has executed (in one of the …Executing methods) then EF will not attempt to actually execute the command, but will instead just use the result set.</span></span> <span data-ttu-id="f5b45-220">Diğer bir deyişle, yakalayıcısı komutun yürütülmesini engelleyebilir, ancak komutun yürütüldüğünden EF ile devam eder.</span><span class="sxs-lookup"><span data-stu-id="f5b45-220">In other words, the interceptor can suppress execution of the command but have EF continue as if the command had been executed.</span></span>  

<span data-ttu-id="f5b45-221">Bunun nasıl kullanılacağına ilişkin bir örnek, bir sarmalama sağlayıcısıyla geleneksel olarak gerçekleştirilen komut toplu işi örneğidir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-221">An example of how this might be used is the command batching that has traditionally been done with a wrapping provider.</span></span> <span data-ttu-id="f5b45-222">Yakalayıcısı, sonraki yürütme için komutu bir toplu iş olarak depolar, ancak komutun normal olarak yürütüldüğünü, "pretend" olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-222">The interceptor would store the command for later execution as a batch but would “pretend” to EF that the command had executed as normal.</span></span> <span data-ttu-id="f5b45-223">Toplu işlem uygulamak için bundan daha fazlasını gerektirdiğini unutmayın, ancak bu durum, ayırma sonucunun nasıl kullanıldığına ilişkin bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-223">Note that it requires more than this to implement batching, but this is an example of how changing the interception result might be used.</span></span>  

<span data-ttu-id="f5b45-224">Yürütme aynı zamanda özel durum özelliği bir... ile ayarlanarak de gizlenebilir. Yöntemler yürütülüyor.</span><span class="sxs-lookup"><span data-stu-id="f5b45-224">Execution can also be suppressed by setting the Exception property in one of the …Executing methods.</span></span> <span data-ttu-id="f5b45-225">Bu, işlemin yürütülmesi verilen özel durumu oluşturarak başarısız olmaya devam etmesine neden olur.</span><span class="sxs-lookup"><span data-stu-id="f5b45-225">This causes EF to continue as if execution of the operation had failed by throwing the given exception.</span></span> <span data-ttu-id="f5b45-226">Bu, kuşkusuz uygulamanın kilitlenmesine neden olabilir, ancak başka bir özel durum veya EF tarafından işlenen diğer özel durumlar da olabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-226">This may, of course, cause the application to crash, but it may also be a transient exception or some other exception that is handled by EF.</span></span> <span data-ttu-id="f5b45-227">Örneğin, bu, komut yürütme başarısız olduğunda bir uygulamanın davranışını test etmek için test ortamlarında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-227">For example, this could be used in test environments to test the behavior of an application when command execution fails.</span></span>  

#### <a name="changing-the-result-after-execution"></a><span data-ttu-id="f5b45-228">Yürütmeden sonra sonucu değiştirme</span><span class="sxs-lookup"><span data-stu-id="f5b45-228">Changing the result after execution</span></span>  

<span data-ttu-id="f5b45-229">Bir dinleyici, komut yürütüldükten sonra Result özelliğini ayarlarsa (bir... Yürütülen Yöntemler) ardından EF, işlemden aslında döndürülen sonuç yerine değiştirilen sonucu kullanır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-229">If an interceptor sets the Result property after the command has executed (in one of the …Executed methods) then EF will use the changed result instead of the result that was actually returned from the operation.</span></span> <span data-ttu-id="f5b45-230">Benzer şekilde, bir dinleyici, komut yürütüldükten sonra özel durum özelliğini ayarlarsa EF, işlem özel durumu oluşturdu gibi set özel durumunu oluşturur.</span><span class="sxs-lookup"><span data-stu-id="f5b45-230">Similarly, if an interceptor sets the Exception property after the command has executed, then EF will throw the set exception as if the operation had thrown the exception.</span></span>  

<span data-ttu-id="f5b45-231">Bir dinleyici, özel durum oluşturulması gerektiğini belirtmek için özel durum özelliğini null olarak da ayarlayabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-231">An interceptor can also set the Exception property to null to indicate that no exception should be thrown.</span></span> <span data-ttu-id="f5b45-232">Bu işlem, işlemin yürütülmesi başarısız olursa, ancak bu işlem başarılı olmuş gibi devam etmek için bu yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-232">This can be useful if execution of the operation failed but the interceptor wishes EF to continue as if the operation had succeeded.</span></span> <span data-ttu-id="f5b45-233">Bu genellikle, sonucun devam ettiği için birlikte çalışmak üzere bir sonuç değeri olması için sonucu ayarlamayı da içerir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-233">This usually also involves setting the Result so that EF has some result value to work with as it continues.</span></span>  

#### <a name="originalresult-and-originalexception"></a><span data-ttu-id="f5b45-234">OriginalResult ve OriginalException</span><span class="sxs-lookup"><span data-stu-id="f5b45-234">OriginalResult and OriginalException</span></span>  

<span data-ttu-id="f5b45-235">EF bir işlem çalıştırdıktan sonra, yürütme başarısız olduysa Result ve OriginalResult özelliklerini ayarlar ve yürütme bir özel durumla başarısız olursa, Exception ve OriginalException özellikleri ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-235">After EF has executed an operation it will set either the Result and OriginalResult properties if execution did not fail or the Exception and OriginalException properties if execution failed with an exception.</span></span>  

<span data-ttu-id="f5b45-236">OriginalResult ve OriginalException özellikleri salt okunurdur ve yalnızca bir işlem yürütüldükten sonra EF tarafından ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="f5b45-236">The OriginalResult and OriginalException properties are read-only and are only set by EF after actually executing an operation.</span></span> <span data-ttu-id="f5b45-237">Bu özellikler, yakalayıcılar tarafından ayarlanamaz.</span><span class="sxs-lookup"><span data-stu-id="f5b45-237">These properties cannot be set by interceptors.</span></span> <span data-ttu-id="f5b45-238">Bu, herhangi bir yakacının, bir özel durumun veya işlem yürütüldüğünde oluşan sonucun aksine, başka bir dinleyici tarafından ayarlanan bir özel durum ya da sonuç arasında ayrım yaptığı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-238">This means that any interceptor can distinguish between an exception or result that has been set by some other interceptor as opposed to the real exception or result that occurred when the operation was executed.</span></span>  

### <a name="registering-interceptors"></a><span data-ttu-id="f5b45-239">Kayıt yaptırıcılar</span><span class="sxs-lookup"><span data-stu-id="f5b45-239">Registering interceptors</span></span>  

<span data-ttu-id="f5b45-240">Bir veya daha fazla savunma arabirimini uygulayan bir sınıf oluşturulduktan sonra, Dbyakaısyon sınıfı kullanılarak EF ile kaydedilebilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-240">Once a class that implements one or more of the interception interfaces has been created it can be registered with EF using the DbInterception class.</span></span> <span data-ttu-id="f5b45-241">Örnek:</span><span class="sxs-lookup"><span data-stu-id="f5b45-241">For example:</span></span>  

``` csharp
DbInterception.Add(new NLogCommandInterceptor());
```  

<span data-ttu-id="f5b45-242">Ayrıca, uygulama etki alanı düzeyinde DbConfiguration kod tabanlı yapılandırma mekanizması kullanılarak da kayıt yapılabilir.</span><span class="sxs-lookup"><span data-stu-id="f5b45-242">Interceptors can also be registered at the app-domain level using the DbConfiguration code-based configuration mechanism.</span></span>  

### <a name="example-logging-to-nlog"></a><span data-ttu-id="f5b45-243">Örnek: NLog günlüğüne kaydetme</span><span class="sxs-lookup"><span data-stu-id="f5b45-243">Example: Logging to NLog</span></span>  

<span data-ttu-id="f5b45-244">Bunu, ıdbcommandyakalayıcısı ve [NLog](https://nlog-project.org/) ' un kullanıldığı bir örneğe bir araya koyalım:</span><span class="sxs-lookup"><span data-stu-id="f5b45-244">Let’s put all this together into an example that using IDbCommandInterceptor and [NLog](https://nlog-project.org/) to:</span></span>  

- <span data-ttu-id="f5b45-245">Zaman uyumsuz olarak yürütülen her komut için bir uyarı Kaydet</span><span class="sxs-lookup"><span data-stu-id="f5b45-245">Log a warning for any command that is executed non-asynchronously</span></span>  
- <span data-ttu-id="f5b45-246">Yürütüldüğünde oluşturan herhangi bir komut için bir hata günlüğe kaydet</span><span class="sxs-lookup"><span data-stu-id="f5b45-246">Log an error for any command that throws when executed</span></span>  

<span data-ttu-id="f5b45-247">Aşağıda gösterildiği gibi kaydedilmesi gereken günlüğe kaydetme sınıfı aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="f5b45-247">Here’s the class that does the logging, which should be registered as shown above:</span></span>  

``` csharp
public class NLogCommandInterceptor : IDbCommandInterceptor
{
    private static readonly Logger Logger = LogManager.GetCurrentClassLogger();

    public void NonQueryExecuting(
        DbCommand command, DbCommandInterceptionContext<int> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void NonQueryExecuted(
        DbCommand command, DbCommandInterceptionContext<int> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    public void ReaderExecuting(
        DbCommand command, DbCommandInterceptionContext<DbDataReader> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void ReaderExecuted(
        DbCommand command, DbCommandInterceptionContext<DbDataReader> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    public void ScalarExecuting(
        DbCommand command, DbCommandInterceptionContext<object> interceptionContext)
    {
        LogIfNonAsync(command, interceptionContext);
    }

    public void ScalarExecuted(
        DbCommand command, DbCommandInterceptionContext<object> interceptionContext)
    {
        LogIfError(command, interceptionContext);
    }

    private void LogIfNonAsync<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        if (!interceptionContext.IsAsync)
        {
            Logger.Warn("Non-async command used: {0}", command.CommandText);
        }
    }

    private void LogIfError<TResult>(
        DbCommand command, DbCommandInterceptionContext<TResult> interceptionContext)
    {
        if (interceptionContext.Exception != null)
        {
            Logger.Error("Command {0} failed with exception {1}",
                command.CommandText, interceptionContext.Exception);
        }
    }
}
```  

<span data-ttu-id="f5b45-248">Bu kodun, zaman uyumsuz olmayan bir komutun yürütüldüğü ve bir komutun yürütüldüğü bir hata olduğunda keşfedildiğinde bulması için yakalanmış bağlamını nasıl kullandığını fark edin.</span><span class="sxs-lookup"><span data-stu-id="f5b45-248">Notice how this code uses the interception context to discover when a command is being executed non-asynchronously and to discover when there was an error executing a command.</span></span>  
